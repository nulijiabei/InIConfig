#include "ini.h"
#include <iostream>

int main()
{

    // INI
    INI * ini = new INI("ini");
    // 读取 -1 Error
    ini->ReadINI();
    cout << << endl;
    cout << ini->GetValByKeys("hello","d") << endl;
    ini->GetValsByKeys("")
    // 释放
    delete(ini);

    return 0;
}
